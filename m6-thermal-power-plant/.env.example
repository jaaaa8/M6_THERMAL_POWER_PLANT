# Dev: để trống hoặc xoá dòng dưới — request đi qua Vite proxy (/api → backend, xem vite.config.js)
# Production (vite build --mode production): BẮT BUỘC set để trỏ thẳng tới backend đã deploy
VITE_API_BASE_URL=http://localhost:8080
